const defaultPropTable = {
  pagination: {
    defaultPageSize: 20,
    pageSizeOptions: ['20', '50', '100'],
    showSizeChanger: true,
    locale: { items_per_page: '' },
  },
};

export default defaultPropTable;
